let activeCategory="all";const names={all:"All products",tech:"Tech picks",home:"Home picks",fitness:"Fitness picks",style:"Style picks"};
function stars(r){return"★".repeat(Math.round(r))+"☆".repeat(5-Math.round(r))}
function render(){const q=(document.getElementById("search").value||"").toLowerCase();const list=PRODUCTS.filter(p=>(activeCategory==="all"||p.category===activeCategory)&&(p.name+" "+p.description+" "+p.bestFor).toLowerCase().includes(q));document.getElementById("resultsTitle").textContent=names[activeCategory];document.getElementById("resultCount").textContent=`${list.length} result${list.length===1?"":"s"}`;document.getElementById("productGrid").innerHTML=list.map(p=>`<article class="card"><a href="product.html?id=${p.id}"><div class="productImage"><img src="${p.image}" alt="${p.name}" loading="lazy"><span class="badge">${p.badge}</span></div></a><h3>${p.name}</h3><div class="stars">${stars(p.rating)} <span class="muted">${p.rating}/5</span></div><p class="muted">${p.description}</p><div class="cardBottom"><span class="price">${p.price}</span><a class="deal" href="${p.affiliateUrl}" target="_blank" rel="nofollow sponsored noopener">Check price ↗</a></div></article>`).join("")||"<p>No products match your search.</p>";document.getElementById("compareBody").innerHTML=PRODUCTS.slice(0,5).map(p=>`<tr><td><strong>${p.name}</strong></td><td>${p.bestFor}</td><td>${p.rating}/5</td><td>${p.price}</td><td><a class="deal" href="product.html?id=${p.id}">Review</a></td></tr>`).join("")}
function setCat(cat){activeCategory=cat;render();document.getElementById("shop").scrollIntoView({behavior:"smooth"})}
function toggleTheme(){document.body.classList.toggle("dark");localStorage.setItem("sp-theme",document.body.classList.contains("dark")?"dark":"light")}
function toggleNav(){const n=document.getElementById("nav");n.style.display=n.style.display==="flex"?"none":"flex"}
function subscribe(e){e.preventDefault();document.getElementById("message").textContent="Thanks — you're on the list! ✨";e.target.reset()}
if(localStorage.getItem("sp-theme")==="dark")document.body.classList.add("dark");render();